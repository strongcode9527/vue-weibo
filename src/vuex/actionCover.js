export const setIsShowCover = function ({ dispatch, state },flag) {
  dispatch('setIsShowCover', flag);
}

export const setIsShowLogin = function ({ dispatch, state },flag) {
  dispatch('setIsShowLogin', flag);
}

export const setIsShowRegister = function ({ dispatch, state },flag) {
  dispatch('setIsShowRegister', flag);
}
